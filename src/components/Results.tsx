import type { Profile, Session } from '../core/types';
import { percent, summarize } from '../core/scoring';
import { modeNames } from '../core/protocol';
import { download } from '../data/export';
import { Crate, Icon } from './Art';
export function Stats({ session }: { session: Session }) {
  const s = summarize(session);
  return <div className="result-stats"><article><span>Order remembered</span><strong>{percent(s.exact)}</strong><small>Whole sequences, in order</small></article><article><span>Peppers sorted</span><strong>{percent(s.sorting)}</strong><small>Correct basket choices</small></article><article><span>Longest recall</span><strong>{s.bestSpan ?? '—'}<em> plants</em></strong><small>Your longest perfect sequence</small></article><article><span>Rounds completed</span><strong>{s.rounds}</strong><small>{Math.round(session.elapsedMs / 60000)} minutes in the garden</small></article></div>;
}
export default function Results({ session, profile, onHome, onAgain, practicePassed = false }: { session: Session; profile: Profile; onHome: () => void; onAgain: () => void; practicePassed?: boolean }) {
  const s = summarize(session), practice = session.protocol.mode === 'practice';
  return <main className="results-page page-enter"><div className="section-heading"><div className="harvest-art"><Crate /><Crate sauce /></div><span className="eyebrow">{modeNames[session.protocol.mode]} · {profile.name}</span><h1>{practice ? practicePassed ? 'You’re ready to grow.' : 'A little practice goes a long way.' : 'A little effort. A lovely harvest.'}</h1><p>{practice ? practicePassed ? 'You’ve practised both parts. Your garden is ready for a full session.' : 'Take your time. Try another practice to put sorting and remembering together.' : 'Fresh peppers for the market. A jar of sauce for later. Time well spent.'}</p></div>
    {!practice && <Stats session={session} />}
    {session.status === 'partial' && !practice && <p className="notice">This session finished early ({session.endReason?.replaceAll('-', ' ')}). Results include {s.rounds} completed, unassisted rounds; unfinished rounds are kept separately.</p>}
    <div className="result-actions"><button className="button button-primary" onClick={onAgain}>{practice ? practicePassed ? 'Start my session' : 'Practise again' : 'Another little harvest'} <Icon name="arrow" /></button><button className="button button-outline" onClick={onHome}>Back to my garden</button></div>
    {!practice && <details className="details-panel"><summary>Look a little closer <span>Scores, timing & session details</span></summary><div className="details-content"><div className="detail-grid"><p>Places in the right position <b>{percent(s.positions)}</b></p><p>Both tasks fully correct <b>{percent(s.strict)}</b></p><p>Mastered span (3 of 5) <b>{s.masteredSpan ?? '—'}</b></p><p>Correct sorting speed, median <b>{s.sortCorrectRT.median === null ? '—' : `${Math.round(s.sortCorrectRT.median)} ms`} <small>n={s.sortCorrectRT.n}</small></b></p><p>First recall, median <b>{s.recallInitiation.median === null ? '—' : `${Math.round(s.recallInitiation.median)} ms`}</b></p><p>Guided / interrupted rounds <b>{s.guided} / {s.interruptions}</b></p><p>Sorting / recall omissions <b>{s.sortOmissions} / {s.recallOmissions}</b></p><p>Retention interval <b>{session.protocol.delayMs / 1000} seconds</b></p></div><div className="table-wrap"><table><thead><tr><th>Sequence length</th><th>Rounds</th><th>Perfect recall</th><th>Correct sorts</th></tr></thead><tbody>{s.bySpan.map(r => <tr key={r.span}><td>{r.span} plants</td><td>{r.rounds}</td><td>{r.exact} / {r.rounds}</td><td>{r.sorts} / {r.opportunities}</td></tr>)}</tbody></table></div><p className="small muted">Protocol {session.protocol.id} v{session.protocol.version} · Configuration {session.protocolHash}. Guided and interrupted rounds stay separate. Response times are browser software estimates.</p></div></details>}
    <div className="export-row"><span><Icon name="download" /> Keep a copy of your harvest</span><button className="text-button" onClick={() => download([session], 'json', [profile])}>Download JSON</button><button className="text-button" onClick={() => download([session], 'csv')}>Download CSV</button></div>
  </main>;
}
export function History({ sessions, profile, onView, onStart }: { sessions: Session[]; profile: Profile; onView: (s: Session) => void; onStart: () => void }) {
  const rows = [...sessions].filter(s => s.participantId === profile.id && s.status !== 'active').sort((a, b) => b.startedAt.localeCompare(a.startedAt));
  const assessments = rows.filter(s => s.protocol.mode === 'assessment' && s.status === 'completed');
  const reference = assessments[0];
  const chartRows = reference ? assessments.filter(s => s.protocolHash === reference.protocolHash && JSON.stringify(s.environment.viewport) === JSON.stringify(reference.environment.viewport) && s.environment.pixelRatio === reference.environment.pixelRatio && s.environment.userAgent === reference.environment.userAgent && JSON.stringify(s.environment.calibration) === JSON.stringify(reference.environment.calibration) && modalitySignature(s) === modalitySignature(reference)).slice(0, 8).reverse() : [];
  return <main className="history-page page-enter"><div className="section-heading align-left"><span className="eyebrow">Your garden journal</span><h1>Little by little, visit by visit.</h1><p>Your harvests, remembered. Showing sessions for {profile.name}.</p></div>
    {rows.length === 0 ? <div className="empty-state"><span className="round-icon"><Icon name="sprout" size={35} /></span><h2>Every garden starts somewhere.</h2><p>Your first harvest will appear here. A few quiet minutes are all you need.</p><button className="button button-primary" onClick={onStart}>Plant the first seed <Icon name="arrow" /></button></div> : <>
      {chartRows.length >= 2 && <section className="trend-card"><div><span className="eyebrow">Matched memory check-ins</span><h2>Your order-recall history</h2><p>Same protocol, screen setup and response methods.</p></div><div className="trend-chart" role="img" aria-label={chartRows.map(s => `${new Date(s.startedAt).toLocaleDateString()}: ${percent(summarize(s).exact)}`).join('; ')}>{chartRows.map(s => <div key={s.id} className="trend-column"><b>{percent(summarize(s).exact)}</b><span style={{ height: `${Math.max(3, (summarize(s).exact ?? 0) * 120)}px` }} /><small>{new Date(s.startedAt).toLocaleDateString(undefined, { month: 'short', day: 'numeric' })}</small></div>)}</div></section>}
      <div className="history-list">{rows.map(s => { const summary = summarize(s); return <button className="history-row" key={s.id} onClick={() => onView(s)}><span className="history-icon"><Icon name={s.protocol.mode === 'assessment' ? 'chart' : 'leaf'} /></span><span className="history-title"><strong>{modeNames[s.protocol.mode]}</strong><small>{new Date(s.startedAt).toLocaleString(undefined, { dateStyle: 'medium', timeStyle: 'short' })} · {s.status === 'partial' ? 'Finished early' : 'Complete'}</small></span><span className="history-score"><b>{percent(summary.exact)}</b><small>order recalled</small></span><span className="history-score"><b>{summary.rounds}</b><small>rounds</small></span><Icon name="arrow" /></button>; })}</div>
      <div className="export-row"><span><Icon name="download" /> Back up {profile.name}’s sessions</span><button className="text-button" onClick={() => download(rows, 'json', [profile])}>All JSON</button><button className="text-button" onClick={() => download(rows, 'csv')}>All CSV</button></div>
    </>}
  </main>;
}
function modalitySignature(s: Session) { return [...new Set(s.trials.flatMap(t => [...t.sorts.map(r => r.modality), ...t.recalls.map(r => r.modality)]).filter(Boolean))].sort().join(','); }
